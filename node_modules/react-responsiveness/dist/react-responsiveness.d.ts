import React from 'react';
import { ReactResponsivenessBreakpoints, ResponsivenessContextValue } from './types';
interface ResponsivenessContextProps {
    children?: React.ReactNode;
    breakpoints?: ReactResponsivenessBreakpoints;
}
export declare const useResponsiveness: () => ResponsivenessContextValue;
export declare const ResponsivenessProvider: React.FC<ResponsivenessContextProps>;
export {};
